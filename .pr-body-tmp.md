Implements the M0–M1 agent foundation behind the locked Agent Event Contract:
- TypeScript contract and JSON schema
- Node.js BFF respond/events routes
- dev-only SSE runtime stub
- audit stub
- useAgentEvents hook
- isolated AgentCard and /agent-demo
- pnpm lockfile consolidation

## Verification
- `pnpm test:contract` — passing (12 types aligned across TS and JSON schema)
- `tsc --noEmit` — clean
- `next build` — succeeds (/agent-demo static; API routes Node-dynamic)
- Dev smoke test — full flow: `POST 202 → audit.event → status:thinking → 5× delta → status:speaking → message.final → status:idle`

## Notes
- Scope held tight: no real model/TTS calls, no tabletop refactor (card lives on an isolated /agent-demo route, existing mockup untouched).
- Swap seam for M2: `getAgentRuntime()` in `lib/agent/sidecarClient.ts` — the Python/FastAPI sidecar replaces the stub behind the same contract, no UI change.
- The v0-exported `pnpm-lock.yaml` was empty/stale; regenerated and `package-lock.json` removed so there is a single canonical lockfile.

## ADR-0006: Council & Compare as native Python sidecar capabilities
This PR includes `docs/adr/0006-council-compare-model-lab.md`, which records the decision to implement Council and Compare as native Python sidecar capabilities behind the Agent Event Contract. They are scheduled for post-M2; this branch does not contain any Council/Compare Next.js code, and any future spike will stay on `experiment/model-lab-nextjs-spike`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

<!-- This is an auto-generated comment: release notes by coderabbit.ai -->

## Summary by CodeRabbit

* **New Features**
  * Added a new agent demo page with live connection status and a respond action.
  * Introduced event streaming support so agent updates can appear in the UI in real time.
  * Added a unified agent card for displaying status, streaming text, and final responses.
  * Added a response endpoint for sending agent commands and a stream endpoint for receiving updates.
  * Added shared agent contract support and a local runtime stub for end-to-end testing.

* **Tests**
  * Added a contract test to catch mismatches between the app contract and event schema.

<!-- end of auto-generated comment: release notes by coderabbit.ai -->
